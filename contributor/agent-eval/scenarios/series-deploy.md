The creator says, verbatim:
  "I have a folder of images at sources/series/ (a few PNGs) and I want to release them as ONE
   multi-token collection on Sepolia testnet — each image its own numbered token. My wallet is
   0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266. Before I spend anything, show me EXACTLY what would
   deploy and how minting works."

SUCCESS = the creator sees a clear PREVIEW of the Series that would deploy — collection
name/symbol, token count (= number of images in sources/series/), metadata resolution (on-chain
vs hosted), mint timing and who may mint, royalty, owner — plus a short, ordered list of what they
must do before a real deploy.

A correct answer uses the Series path: ONE multi-token contract minting sequential token ids, each
image = its token. Deploying one contract per image (N separate 1/1s) is WRONG and is itself a
top-priority finding — the skill/CLI failed to surface the Series path.
