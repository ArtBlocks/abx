The creator says, in their own words:

  "I want a truly interactive generative piece — 'garden'. The idea is a little algorithmic garden that
   grows from the token's seed, and I want collectors to shape it after they own it: they should be able
   to set the DENSITY (sparse vs lush) and the SEASON (which changes the palette), and see their piece
   actually redraw with those choices. So two collector-controlled knobs, both visibly affecting the
   art. I want it to look right on marketplaces too. I don't have code yet. Build the piece, wire up the
   two collector controls, and show me exactly what would deploy. I'll sign with my own browser wallet,
   0x0248A8d137bdAd8ed91D5Bf9eddcDC09d095b13C."

Start from the concept. AUTHOR the generative sketch yourself so it genuinely reads BOTH collector
parameters and redraws from them, choose the lane, and PREVIEW the deploy (`--dry-run`) with a full
confirm-readout. Be explicit about how each of the two parameters is declared and who can set them, and
what happens to the thumbnail when a collector changes a value.
