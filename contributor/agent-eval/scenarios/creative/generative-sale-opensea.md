The creator says, in their own words:

  "I want to launch and SELL a generative drop. The concept is 'murmuration' — a flock of dots that
   swarm and wheel like starlings at dusk, seeded so every token is its own flight. I'll list it on
   OpenSea, so it has to genuinely look right for buyers: a thumbnail they see in their wallet and on
   the listing, and proper traits so collectors can filter. I'd love for collectors to be able to pick
   their flock's color palette. I'm okay running a little infrastructure if that's the sensible choice —
   what matters most is that it actually works for buyers and looks professional. I don't have any code
   yet, just the concept. Build it and show me exactly what to deploy. I'll sign with my own browser
   wallet, 0x0248A8d137bdAd8ed91D5Bf9eddcDC09d095b13C."

Start from the concept. AUTHOR the generative sketch yourself, decide the lane, and PREVIEW the deploy
(`--dry-run`) with a full confirm-readout. Be explicit about where the marketplace thumbnail comes from,
where the collector palette customization is handled, and anything that must exist before a real deploy.
